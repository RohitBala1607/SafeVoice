# API Server

This will be the main backend API server integrated with MongoDB.

## Flow:
1. User Auth (Register/Login)
2. Handle Emergency Contacts
3. Process Complaints / SOS Triggers
4. Store data in MongoDB Atlas
5. Interface with `ai-service` for priority prediction
